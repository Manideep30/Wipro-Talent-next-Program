


/*Run the below script Create table MY_EMPLOYEE as
Select employee_id, first_name, last_name, department_id, salary from EMPLOYEES where 1=2;*/


CREATE TABLE myEmployee (empId INT, firstName VARCHAR(10), lastName VARCHAR(10), 
deptId INT, salary FLOAT);

